# Frozen rooted-estate reference: consumption boundary

Reference track: **`68923b4c326fcaf26fad59fa1b87c91d1890802f`**, stopped.
The supplied session handoff is
`files/greenfield-rooted-estate-contracts/README.md`, with `CONTRACTS.md`,
framework-neutral illustrative ports, acceptance matrix and explicit blockers.
It is design/reference evidence, **not** canonical authority, a transport
format, a dependency or greenfield acceptance.

No reference-app code, `WorkspaceSummary`, `HostServices`, identity file schema,
directory layout, Node executor, UI or alternative kernel is imported. The
illustrative branded TypeScript handles do not authenticate anything at runtime.

## Identity and readiness

The original full `identity.body_stream` RAPPID is the GUID in reference forms
where that binding applies. Greenfield resolves its full GUID from the verified
original body stream; its existing `root` projection field carries that same
full value, not a second identity. Unknown forms must use their actual adopted
binding or remain unavailable. UUIDs, `agentId`, tails, scope names, URLs and
display labels are not substitutes.

`egg.at-rest` now exposes this read-only `identity.body_stream` alongside its
existing byte commitments, plus explicit **false** local diagnostics:

```text
canonicalClosureAvailable: false
domainBinding: false
codeLoading: false
sharedRuntime: false
transferAuthority: false
transformation: false
```

These are inspection diagnostics, not RAPP/1 wire fields or deserializable
authority. A keyed root, matching capability SHA-256, passing frame checker,
fixture runtime, local shared-spine contract or supplied boolean cannot turn
them true. Complete adopted form/world closure, domain/protection, code-loading
and existing-interpreter bindings remain owner-activation prerequisites.

## Preserve the world; do not manufacture closure

The supported new-core root contract preserves its complete recursive scope
tree, all supplied canonical frames, original artifact/capability data, source
streams, hidden state and branches. The migration fixture exercises all required
forms: bots/worlds, workspaces, agents, Twins, neighborhoods, factories,
rapplications, tasks, routines, memories, artifacts and evidence.

This is not proof of universal registered-form closure for arbitrary old
archives. Unsupported forms/backing relationships remain honestly unavailable,
not flattened into workspace-shaped DTOs or promoted from repository paths.
Source-owned internal conversations remain on their original streams.
Aggregate views only resolve references.

Egg remains the **byte-identical rooted tree at rest**. The only inclusion
intents are GODD, DOGG or both under adopted canonical authority. No new Egg
format, frame filtering, rewritten chains, default-world IDs, code execution or
surrogate identities are introduced. `egg.transfer` continues to refuse all
inspect/export/restore scopes until independent owner activation and complete
bindings exist. A carried `ownerActivation:true` is rejected, not trusted.

## Acceptance mapping, not inherited green badges

| Reference contracts | Current greenfield evidence | Still unavailable |
|---|---|---|
| C01–C02: authority/identity | Independently selected exact rev-15 authority/checker; nonzero emitted frames; exact-root and alias-refusal tests | Live owner/registry/profile adoption and independent anti-rollback anchors |
| C03–C05: complete world/domain/root surface | Full supported fixture tree/branches and original byte preservation; headless root-only identity; no product UI | Universal form/default-world/external-backing closure and adopted domain selection/protection |
| C06–C07: observer/lifecycle | Dormancy, observer non-mutation, root-isolated cancellation, hide/restore and restart tests | Adopted external lifecycle/runtime handling for unsupported source profiles |
| C08: exact-tree transfer | Sanitized API-only atomic whole-root migration, original byte comparison, rollback/restart proof | General Egg export/restore, held quiescent adopted closure, cross-device activation |
| C09: shared interpreter | Target-owned verified adapter boundary and injected multi-root isolation tests; external grail unchanged | Actual adopted existing-Brainstem materialization/code-loading interface |
| C10: collaboration | Signed fixture guidance/echo, distinct public perspectives and reference-only continuity | Production disclosure/exchange adoption and live signer custody |
| C11: channels | Default-off private transport; source-reference recaps and synthetic idempotent delivery tests | Actual private sender/contact/permission custody and approved transport |
| C12: evidence/stopping | Separate current receipts and explicit false readiness diagnostics | Owner activation; fixture/reference results never grant it |

The reference's historical rev-14 pin and later rev-15 trace are not relabeled
or silently promoted. Greenfield retains its already independently verified
rev-15 selection at `dda32d741c7218f41443a5bd17eebfe0eae82cb7`, head
`83ca275f35cca96e43d75c99d338326c1a39b2240eabf57eb7c29ac96cc90818`.

Private contact/credential/runtime state is not portable truth. New channel
bindings keep all private material in a strict same-user runtime sibling;
canonical records retain only an opaque root/operation reference and public
policy. Old contact-bearing test/history records cannot activate delivery.
Hashing a contact is not a privacy or export grant. Transfer remains refused;
no old records are filtered or rewritten to disguise missing adoption.

Reference checksums verify handoff-file integrity only. Its compile-only type
tests and historical app tests are not counted as greenfield behavioral,
cryptographic, migration or release success. The reference remains stopped;
no merge, profile read, activation or installation follows from consuming it.
